using Vienna;
using Vienna.Actors;

namespace ViennaConsole
{
    public class TestActors
    {
        public void Execute()
        {
            //var text = Helper.LoadFile("assets/monster.json");
            //var factory = new Actor.Factory();

            //Helper.StartTimer();

            //var json = JObject.Parse(text);

            //Logger.Debug("parse json -> {0}", Helper.StopTimer());

            //Helper.StartTimer();

            //factory.Create(json);

            //Logger.Debug("factory -> {0}", Helper.StopTimer());

            //Logger.Debug(sizeof(long));

        }
    }
}
